{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.MontyGovernance/montycat-mcp",
  "title": "Montycat MCP — Shared AI Memory",
  "description": "One shared, persistent memory across AI agents and systems, with semantic recall and live updates.",
  "repository": {
    "url": "https://github.com/MontyGovernance/montycat-mcp",
    "source": "github"
  },
  "version": "0.5.1",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "montycat-mcp",
      "version": "0.5.1",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "MONTYCAT_URI",
          "description": "Optional Montycat engine URI: montycat://user:password@host:port/store",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "MONTYCAT_TLS",
          "description": "Set to true when connecting to a TLS-enabled remote Montycat engine",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        }
      ]
    },
    {
      "registryType": "oci",
      "identifier": "docker.io/montygovernance/montycat-mcp:0.5.1",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "MONTYCAT_URI",
          "description": "Optional Montycat engine URI: montycat://user:password@host:port/store",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "MONTYCAT_TLS",
          "description": "Set to true when connecting to a TLS-enabled remote Montycat engine",
          "isRequired": false,
          "isSecret": false,
          "format": "string"
        }
      ]
    }
  ]
}
